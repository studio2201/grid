[package]
name = "backend"
version = "1.0.2"
edition = "2024"
authors = ["UberMetroid <admin@ubermetroid.com>"]
license = "GPL-3.0-only"
repository = "https://github.com/UberMetroid/RustKan"
homepage = "https://github.com/UberMetroid/RustKan"
description = "Axum backend service for RustKan."
readme = "../README.md"

[dependencies]
tokio = { version = "1.36", features = ["macros", "rt-multi-thread", "net", "time", "io-util"] }
axum = "0.7"
tower-http = { version = "0.5", features = ["fs", "cors", "trace"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
dotenvy = "0.15"
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }
reqwest = { version = "0.11", default-features = false, features = ["json", "rustls-tls"] }
chrono = { version = "0.4", default-features = false, features = ["std", "serde", "clock"] }
regex = "1.10"
indexmap = { version = "2.2", features = ["serde"] }
sha2 = "0.10"
ipnet = "2.9"


